{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix app ci --include=dev && npm --prefix app run build",
  "outputDirectory": "app/dist",
  "installCommand": "echo \"install happens in buildCommand, scoped to app/\"",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://remain-mtkz.onrender.com/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
